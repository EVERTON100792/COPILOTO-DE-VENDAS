# OpenRouter (opcional — fica funcionando sem chave via motor local)
# A chave também pode ser cadastrada em Configurações no app (salva no navegador).
OPENROUTER_API_KEY=

# Supabase (próxima fase — persistência em nuvem)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=